{
    "output_dir": "$CHECKPOINT_DIR/$MODEL_TYPE/dp_lora/$LR'_'$EPSILON'_'$SCALE'_c'$CNORM'_b'$BSIZE'_n'$NEGK'_r'$RANK'_z'$DPTYPE",
    "logging_dir": "$LOG_DIR/$MODEL_TYPE/$LR",
    "model_name_or_path": "$MODEL_NAME",
    "model_type": "$MODEL_TYPE",
    "tokenizer_name": "$TOKEN_NAME",
    "do_train": true,
    "save_steps": "$STEPS",
    "eval_steps": "$STEPS",
    "logging_steps": "$(($STEPS/2))",
    "train_path": "$PROCESSED_DIR/train.text.new.jsonl",
    "eval_path": "$EVAL_DIR",
    "corpus_path": "$PROJ_DIR/data/$SDOMAIN/corpus.txt",
    "num_train_epochs": 1,
    "per_device_train_batch_size": "$BSIZE",
    "per_device_eval_batch_size": 256,
    "max_physical_batch_size": 16,
    "learning_rate": "$LR",
    "lr_scheduler_type": "cosine",
    "warmup_ratio": "$RATIO",
    "max_len": 32,
    "save_total_limit": 3,
    "evaluation_strategy": "steps",
    "metric_for_best_model": "eval_mrr",
    "remove_unused_columns": false,
    "overwrite_output_dir": true,
    "report_to": "wandb",
    "seed": 2024,
    "set_pad_id": true,
    "epsilon": "$EPSILON",
    "max_pgrad_norm": "$CNORM",
    "noise_scale": "$SCALE",
    "preclip": 0,
    "neg_k": "$NEGK",
    "dp_type": "$DPTYPE",
    "lora_rank": "$RANK",
    "start_eval": 2500,
    "use_peft": true,
    "quantization": false
  }
